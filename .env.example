# DentraOS shared local environment (safe template only)

# Backend server
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
JWT_SECRET=change-me-in-local-env
APP_TZ=Asia/Kolkata
CLINIC_START=09:00:00
CLINIC_END=18:00:00

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=dentra
DB_PASSWORD=dentra_pass
DB_NAME=dental_clinic

# Frontend API target
VITE_API_BASE_URL=http://localhost:4000

# Worker/queue behavior
WORKER_ID=worker-1
POLL_MS=1200
LOCK_TTL_SECONDS=60
MAX_EVENT_ATTEMPTS=8
NO_SHOW_GRACE_MIN=15
EXPIRY_HORIZON_DAYS=30
AR_REMINDER_DAYS=3
INVENTORY_MONITOR_INTERVAL_MIN=60
REVENUE_MONITOR_INTERVAL_MIN=60
CASE_MONITOR_INTERVAL_MIN=1440

# Optional assistant service and model settings
ASSISTANT_URL=http://127.0.0.1:8010/assistant/message
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional SMTP/Brevo (leave empty for local unless needed)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=0
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
BREVO_API_KEY=
SENDINBLUE_API_KEY=
